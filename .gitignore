node_modules
.env
data/
docs/
.wallets.json
.cursor/

# Cache de velas das sondas (re-fetchavel; o SHA fica no meta.json da sonda).
# Os ensaios antigos vivem em archive/ desde o H1, e o padrao antigo apontava para provas/.
archive/**/velas-*.json
